import { CustomStrDirective } from './custom-str.directive';

describe('CustomStrDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomStrDirective();
    expect(directive).toBeTruthy();
  });
});
